# Backend API URL (server-side only, used in API routes)
TUITIONI_API=http://localhost:8000

# Backend API URL (client-side, exposed to browser for admin dashboard)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Public site URL (used for SEO sitemap/robots.txt)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
